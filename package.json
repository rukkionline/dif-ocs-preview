{
  "name": "dif-ocs-preview",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-preview.mjs",
    "serve": "python3 -m http.server 4317 --bind 127.0.0.1 --directory public",
    "check": "node scripts/check-preview.mjs"
  }
}
