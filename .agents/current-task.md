# Current Task

Активная задача: `.agents/tasks/2026-09-12-agent-knowledge.md`.
Обязательно прочитать state: `.agents/tasks/2026-09-12-agent-knowledge.state.json`.
Git-ветка при baseline миграции: `mac-main` (перепроверить перед правками).
Runtime и активного агента определять из текущей сессии, не из исторической подписи `codex`.

Предыдущая завершённая задача: `.agents/tasks/2026-07-19-dif-ocs-pixel-copy.md`
(status: completed). Отдельная работа: эта миграция её не переоткрывает и не меняет
её owner/release scope.